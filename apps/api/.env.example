# ── TruckNet India API — Environment Variables ──
# Copy this file to .env and fill in real values.
# SECURITY: Never commit .env to version control!

# ── Required ──
PORT=5000
DATABASE_URL=mongodb://127.0.0.1:27017/trucknet?directConnection=true
JWT_SECRET=your-jwt-secret-here
JWT_REFRESH_SECRET=your-jwt-refresh-secret-here
NODE_ENV=development

# ── CORS ──
# REQUIRED in production — set to your Vercel deployment URL
# Example: https://trucknet-india.vercel.app
CORS_ORIGIN=http://localhost:3000

# ── AI Providers (all optional — app falls back gracefully) ──
# Get key from https://console.groq.com/keys
# GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxx

# Ollama local host (install from https://ollama.com/)
# OLLAMA_HOST=http://localhost:11434

# Python AI Engine URL (REQUIRED in production — no localhost default)
AI_ENGINE_URL=http://localhost:8000
